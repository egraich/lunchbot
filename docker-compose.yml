services:
  meal-bot:
    build: .
    container_name: meal-tracker
    restart: unless-stopped
    env_file: .env
    environment:
      DB_PATH: /app/data/meal_tracker.db
    volumes:
      - ./data:/app/data
