services:
  lunch-bot:
    build: .
    container_name: lunch-bot
    restart: unless-stopped
    env_file: .env
    networks:
      - postgres_net

networks:
  postgres_net:
    external: true
    name: postgres_net
